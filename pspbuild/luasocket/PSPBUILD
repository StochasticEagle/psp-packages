pkgname=luasocket
pkgver=3.1.0
pkgrel=7
pkgdesc="Network support for the Lua language"
arch=(any)
url="https://lunarmodules.github.io/luasocket/"
license=('MIT')
groups=('psp-libraries')
depends=('lua55')
makedepends=()
optdepends=()
source=(
    "${pkgname}-${pkgver}::git+https://github.com/StochasticEagle/psp-luasocket.git#commit=7acc95c71294d7100aba7d5b12c1b1d29191c8dc"
)
sha256sums=(
    'SKIP'
)

build() {
    cd "${pkgname}-${pkgver}"
    make PLAT=psp LUAV=5.5
}

package() {
    cd "${pkgname}-${pkgver}"
    make install-static DESTDIR="$pkgdir" PLAT=psp LUAV=5.5

    mkdir -m 755 -p "$pkgdir/psp/share/licenses/$pkgname"
    install -m 644 LICENSE "$pkgdir/psp/share/licenses/$pkgname"
}
